# OS/Editor Noise
.DS_Store
.vscode/
.idea/

# Claude Code local settings (machine-specific, not for repo)
.claude/settings.local.json

# Identity Layer & Cryptography (CRITICAL)
spire/data/
spire/socket/
*.sqlite3
*.sock
*.pem
*.key

# Environment Variables & Secrets
.env
.env.*
!.env.example
security/secrets/
# Just in case you have secrets in the old path
secrets/
cookies.txt

# Python & Orchestration (LangGraph / MCP)
__pycache__/
*.py[cod]
venv/
.venv/

# Edge Inference (Ollama / vLLM)
models/
*.gguf

# Shared Context Store & Docker Data
shared_context/data/
shared_context/*.rdb
docker_data/
*.log

# Claude Code project context (internal)
.claude/
